"""
With Docker Neo4J and Weaver Instance running....
Can run `extract_artifact_names_from_api.py`
to generate artifacts.csv
which contains a list of all artifacts and names

After artifacts are generated, you can run
`enrich_artifact_data.py`
which will pull CVE information for each artifact, and create a file containing additional artifact metadata

"""
